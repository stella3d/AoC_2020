use crate::files;

fn solution(lines: Vec<String>) {
    let mut valid_count = 0;
    for line in lines {

    }

    println!("day 3 - VALID COUNT: {}", valid_count);
}

pub(crate) fn run() {
    solution(files::get_lines("./input/3in.txt").unwrap());
}
